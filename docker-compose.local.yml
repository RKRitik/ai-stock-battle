services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    image: ai-stock-battle:local
    command: bun run dev
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
